import { useNavigate } from 'react-router-dom'

export const LoginPage: React.FC = () => {
  const router = useNavigate()

  return <main className="h-screen bg-zinc-900 pt-[80px] md:px-10 p-4"></main>
}
